---
id: prompt-000                        # Unique prompt ID. Format: prompt-[area]-[slug] e.g. prompt-research-source-extraction
title: ""                             # Descriptive title of what this prompt does
use_case: ""                          # The specific task this prompt is designed to perform e.g. "Extract structured process steps from a government web page"
target_tool: ""                       # The AI tool or agent this prompt is written for e.g. Claude, GPT-4, Perplexity, custom agent
created: ""                           # ISO date: YYYY-MM-DD
updated: ""                           # ISO date: YYYY-MM-DD. Update whenever the prompt is revised.
tags:
  - ""                                # Tags from tag-registry.yaml. Include the area and use-case type (e.g. research, content, extraction, analysis).
---

## Context

<!-- Describe the situation in which this prompt is used. Include:
- What the operator knows before running this prompt
- What inputs are typically available (documents, URLs, raw notes, etc.)
- Any system-level context or role the AI should adopt

This section sets the scene so that anyone picking up this prompt understands when and why to use it. -->

---

## Objective

<!-- State clearly and precisely what this prompt is designed to produce. A single sentence is ideal. Examples:
- "Extract all numbered steps from a government process page and return them as a structured YAML block."
- "Generate a first-draft content brief for a guide targeting EU nationals researching Cyprus residence permits."
- "Identify and list any contradictions between two research sources on the same topic."

Be specific. Vague objectives produce vague outputs. -->

---

## Instructions

<!-- Paste the full prompt text here, exactly as it should be given to the AI. Use [PLACEHOLDERS] in square brackets for any variable inputs the operator must fill in before running.

Write the prompt in the second person (addressing the AI directly). Include:
- The role or persona the AI should adopt (if relevant)
- Step-by-step instructions for what to do
- Any constraints on tone, format, or length
- What to do if information is missing or ambiguous

---
[PASTE PROMPT HERE]

---
-->

---

## Output Format

<!-- Describe exactly what a correct output from this prompt looks like. Include:
- The format (YAML, Markdown, plain text, table, JSON, etc.)
- The structure (fields, headings, sections, numbering)
- Length expectations
- Any fields that must always be present vs. optional

If the output feeds into a specific template or system, reference that template here. -->

---

## Example Output

<!-- Provide a realistic example of a well-formed output from this prompt. This is the single most important section for evaluating prompt quality — if the example is vague, the prompt needs more work.

The example should use realistic (but fictional or anonymised) data. It should demonstrate exactly the structure and quality expected. -->

```
[PASTE EXAMPLE OUTPUT HERE]
```
